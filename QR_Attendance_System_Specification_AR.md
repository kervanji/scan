# نظام حضور وانصراف الموظفين عبر QR Code

## 1. فكرة النظام

تطبيق يعمل على **Windows** ويُستخدم كجهاز تسجيل حضور وانصراف للموظفين.

يقوم الموظف بعرض بطاقة **QR Code** أمام كاميرا الجهاز، ثم يقوم النظام بما يلي:

1. قراءة رمز QR.
2. التحقق من بيانات الموظف.
3. تحديد ما إذا كانت العملية دخولًا أو خروجًا.
4. التقاط صورة للموظف أثناء العملية.
5. حفظ وقت الدخول أو الخروج محليًا على الجهاز.
6. حفظ مسار الصورة للرجوع إليها لاحقًا.
7. إرسال إشعار إلى Telegram يحتوي على اسم الموظف والصورة ووقت العملية.

---

## 2. التقنيات المقترحة

يمكن بناء النظام بالكامل باستخدام Java:

- **Java 17 أو Java 21**
- **JavaFX** لتصميم واجهة البرنامج
- **ZXing** لقراءة وإنشاء QR Code
- **OpenCV** لتشغيل الكاميرا والتقاط الصور
- **SQLite** لحفظ البيانات محليًا
- **Telegram Bot API** لإرسال الإشعارات والصور
- **Apache POI** لتصدير تقارير Excel
- **jpackage** أو **Inno Setup** لإنشاء ملف تثبيت Windows

---

## 3. طريقة عمل النظام

### تسجيل الدخول

1. يختار الموظف زر **تسجيل دخول** أو يعمل النظام في الوضع التلقائي.
2. يعرض الموظف بطاقة QR أمام الكاميرا.
3. يقرأ النظام الرمز ويتحقق من الموظف.
4. يلتقط النظام صورة من الكاميرا.
5. يسجل التاريخ والوقت ونوع العملية.
6. يحفظ السجل والصورة محليًا.
7. يرسل إشعارًا إلى Telegram.

مثال على إشعار الدخول:

```text
✅ تسجيل دخول

الموظف: أحمد محمد
القسم: الإدارة
التاريخ: 10 يوليو 2026
الوقت: 08:03 صباحًا
الجهاز: بوابة المدرسة الرئيسية
```

### تسجيل الخروج

1. يعرض الموظف QR أمام الكاميرا.
2. يلتقط النظام صورة جديدة.
3. يسجل وقت الخروج.
4. يحسب مدة الدوام منذ آخر دخول مفتوح.
5. يرسل إشعارًا إلى Telegram.

مثال:

```text
🔴 تسجيل خروج

الموظف: أحمد محمد
القسم: الإدارة
وقت الخروج: 03:14 مساءً
مدة الدوام: 7 ساعات و11 دقيقة
```

---

## 4. واجهة البرنامج الرئيسية

الواجهة الرئيسية يجب أن تكون بسيطة وواضحة:

```text
┌─────────────────────────────────────────┐
│       نظام حضور وانصراف الموظفين       │
│                                         │
│             عرض الكاميرا                │
│                                         │
│      ضع بطاقة QR أمام الكاميرا          │
│                                         │
│  [ تسجيل دخول ]      [ تسجيل خروج ]    │
│                                         │
│ آخر عملية: أحمد محمد - دخول 08:03       │
└─────────────────────────────────────────┘
```

### عناصر الواجهة

- عرض مباشر للكاميرا.
- زر تسجيل دخول.
- زر تسجيل خروج.
- حالة اتصال الكاميرا.
- حالة اتصال الإنترنت.
- حالة اتصال Telegram.
- آخر موظف قام بالتسجيل.
- رسالة نجاح أو فشل واضحة.
- صوت عند نجاح القراءة أو عند وجود خطأ.

---

## 5. لوحة الإدارة

يجب حماية لوحة الإدارة بكلمة مرور أو PIN.

### الوظائف الأساسية

- إضافة موظف جديد.
- تعديل بيانات الموظف.
- تعطيل الموظف.
- إنشاء QR Code للموظف.
- طباعة بطاقة الموظف.
- مشاهدة حضور اليوم.
- مشاهدة المتأخرين.
- مشاهدة الغائبين.
- مراجعة صور الدخول والخروج.
- تعديل سجل خاطئ.
- إضافة ملاحظة على السجل.
- تصدير التقارير إلى Excel.
- أخذ نسخة احتياطية.
- ضبط إعدادات الكاميرا.
- ضبط إعدادات Telegram.
- ضبط أوقات الدوام والتأخير.

---

## 6. قاعدة البيانات

يتم استخدام قاعدة بيانات SQLite محلية.

### جدول الموظفين

اسم الجدول:

```sql
employees
```

الحقول المقترحة:

```sql
CREATE TABLE employees (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    employee_code TEXT NOT NULL UNIQUE,
    full_name TEXT NOT NULL,
    department TEXT,
    job_title TEXT,
    qr_token TEXT NOT NULL UNIQUE,
    profile_photo_path TEXT,
    is_active INTEGER NOT NULL DEFAULT 1,
    created_at TEXT NOT NULL
);
```

### جدول الحضور والانصراف

اسم الجدول:

```sql
attendance_events
```

الحقول المقترحة:

```sql
CREATE TABLE attendance_events (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    employee_id INTEGER NOT NULL,
    event_type TEXT NOT NULL,
    event_time TEXT NOT NULL,
    photo_path TEXT,
    telegram_sent INTEGER NOT NULL DEFAULT 0,
    device_name TEXT,
    notes TEXT,
    created_by TEXT DEFAULT 'SYSTEM',
    FOREIGN KEY (employee_id) REFERENCES employees(id)
);
```

القيم الممكنة لحقل `event_type`:

```text
CHECK_IN
CHECK_OUT
```

### جدول الإعدادات

```sql
CREATE TABLE settings (
    setting_key TEXT PRIMARY KEY,
    setting_value TEXT
);
```

أمثلة على الإعدادات:

```text
telegram_bot_token
telegram_chat_id
camera_id
device_name
photos_folder
backup_folder
work_start_time
late_after_time
scan_cooldown_seconds
```

### جدول سجل التعديلات

لمنع التلاعب، يجب تسجيل كل تعديل إداري:

```sql
CREATE TABLE audit_logs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    admin_username TEXT NOT NULL,
    action_type TEXT NOT NULL,
    target_table TEXT,
    target_id INTEGER,
    old_value TEXT,
    new_value TEXT,
    action_time TEXT NOT NULL
);
```

---

## 7. محتوى QR Code

لا يُنصح بوضع رقم الموظف فقط داخل QR.

مثال غير آمن:

```text
EMP001
```

مثال أفضل:

```text
EMP-6f21c7d9-40e8-4c71-8df7-9e42684b32ab
```

يتم إنشاء رمز عشوائي وفريد لكل موظف وتخزينه في الحقل:

```text
qr_token
```

عند قراءة QR، يبحث النظام عن الموظف بواسطة هذا الرمز.

---

## 8. تحديد الدخول والخروج

### الطريقة الأولى: أزرار دخول وخروج

تظهر زران على الشاشة:

```text
[ تسجيل دخول ]    [ تسجيل خروج ]
```

يختار الموظف نوع العملية ثم يعرض QR.

هذه الطريقة أكثر دقة وأقل عرضة للأخطاء.

### الطريقة الثانية: الوضع التلقائي

- إذا لم يكن لدى الموظف دخول مفتوح في اليوم نفسه، يسجل النظام دخولًا.
- إذا كان لديه دخول دون خروج، يسجل النظام خروجًا.
- بعد الخروج، يمكن السماح بدخول جديد بعد مدة محددة.

يفضل توفير الطريقتين من إعدادات البرنامج.

---

## 9. التقاط الصور

عند نجاح قراءة QR:

1. ينتظر النظام مدة قصيرة، مثل 300 إلى 700 مللي ثانية.
2. يلتقط صورة واضحة من الكاميرا.
3. يحفظ الصورة بصيغة JPG.
4. يحفظ مسار الصورة في قاعدة البيانات.

### هيكل المجلدات

```text
AttendanceSystem/
├── app/
├── database/
│   └── attendance.db
├── photos/
│   ├── 2026-07-10/
│   │   ├── EMP001_CHECK_IN_08-03-12.jpg
│   │   └── EMP001_CHECK_OUT_15-14-09.jpg
├── backups/
├── logs/
└── config/
```

لا يُفضل حفظ الصور نفسها داخل SQLite، بل يتم حفظ مسار الصورة فقط.

---

## 10. Telegram Bot

### المتطلبات

- إنشاء Bot بواسطة `BotFather`.
- الحصول على `BOT_TOKEN`.
- الحصول على `CHAT_ID`.
- إضافة البوت إلى المجموعة المطلوبة إذا كانت الإشعارات ستصل إلى مجموعة.

### البيانات المطلوبة

```text
BOT_TOKEN
CHAT_ID
```

### الإرسال

يستخدم النظام Telegram Bot API لإرسال:

- صورة العملية.
- اسم الموظف.
- القسم.
- نوع العملية.
- التاريخ.
- الوقت.
- اسم جهاز التسجيل.
- مدة الدوام عند الخروج.

### التعامل مع انقطاع الإنترنت

إذا لم يكن الإنترنت متاحًا:

1. يتم حفظ العملية محليًا.
2. يتم وضع:

```text
telegram_sent = 0
```

3. يقوم النظام بمحاولة إعادة الإرسال دوريًا.
4. بعد نجاح الإرسال يتم تحديث القيمة إلى:

```text
telegram_sent = 1
```

بهذا لا تضيع إشعارات الحضور عند انقطاع الإنترنت.

---

## 11. منع تكرار القراءة

يجب منع قراءة QR نفسه عدة مرات خلال ثوانٍ قليلة.

مثال:

```text
scan_cooldown_seconds = 30
```

إذا قرأ الموظف البطاقة مرة أخرى خلال 30 ثانية، تظهر رسالة:

```text
تم تسجيل العملية مسبقًا، يرجى الانتظار.
```

---

## 12. الأمان ومنع التلاعب

بطاقة QR يمكن تصويرها وإرسالها إلى موظف آخر، لذلك التقاط الصورة مهم للمراجعة.

### الإجراءات المقترحة

- التقاط صورة عند كل عملية.
- عرض صورة الموظف الرسمية بعد قراءة QR.
- منع تكرار القراءة السريع.
- تسجيل محاولات QR غير الصالحة.
- حماية لوحة الإدارة بكلمة مرور.
- عدم السماح بحذف سجلات الحضور مباشرة.
- استخدام سجل تدقيق لكل تعديل.
- تسجيل اسم المسؤول الذي عدل السجل.
- تشفير إعدادات Telegram Bot قدر الإمكان.
- عمل نسخة احتياطية يومية.
- منع تغيير ساعة Windows من الموظفين.
- تسجيل وقت النظام وأي فرق غير طبيعي في الساعة.
- تشغيل التطبيق بوضع Full Screen أو Kiosk Mode.
- تشغيل التطبيق تلقائيًا مع Windows.

### تطوير أمني مستقبلي

يمكن لاحقًا إضافة:

- التعرف على الوجه.
- مطابقة الصورة مع صورة الموظف.
- قارئ بطاقات NFC.
- بصمة إصبع.
- مزامنة مع خادم مركزي.
- لوحة متابعة عبر الويب.

---

## 13. التقارير

### التقارير المطلوبة

- حضور اليوم.
- غياب اليوم.
- المتأخرون.
- الخروج المبكر.
- ساعات دوام الموظفين.
- سجل موظف خلال فترة.
- العمليات غير المرسلة إلى Telegram.
- التعديلات اليدوية.
- تقرير شهري.

### تصدير Excel

يحتوي التقرير على:

```text
رقم الموظف
اسم الموظف
القسم
التاريخ
وقت الدخول
وقت الخروج
مدة الدوام
حالة التأخير
ملاحظات
```

---

## 14. النسخ الاحتياطي

يقوم النظام يوميًا بنسخ:

- قاعدة البيانات.
- إعدادات النظام.
- الصور الجديدة.

مثال:

```text
backups/
└── 2026-07-10/
    ├── attendance.db
    └── config.json
```

يمكن إضافة خيار نسخ احتياطي إلى:

- USB.
- قرص آخر.
- Google Drive.
- OneDrive.
- خادم داخلي.

---

## 15. تشغيل البرنامج على Windows

### المتطلبات

- Windows 10 أو Windows 11.
- كاميرا ويب بدقة 720p أو أعلى.
- ذاكرة RAM لا تقل عن 4GB.
- مساحة تخزين مناسبة للصور.
- اتصال إنترنت لإرسال Telegram.
- Java Runtime مدمج مع التطبيق.

### إعداد التشغيل

- إنشاء Installer.
- إنشاء اختصار على سطح المكتب.
- تشغيل البرنامج تلقائيًا مع Windows.
- فتحه بوضع الشاشة الكاملة.
- منع الموظفين من إغلاق البرنامج دون PIN.
- تشغيل نسخة احتياطية تلقائية يوميًا.

---

## 16. مراحل تطوير النسخة الأولى MVP

### المرحلة الأولى: قاعدة النظام

- إنشاء مشروع JavaFX.
- ربط SQLite.
- إنشاء جداول قاعدة البيانات.
- إنشاء شاشة تسجيل الدخول للإدارة.
- إنشاء شاشة الموظفين.

### المرحلة الثانية: QR والكاميرا

- تشغيل الكاميرا.
- قراءة QR بواسطة ZXing.
- إنشاء QR لكل موظف.
- التقاط الصورة.
- حفظ الصورة محليًا.

### المرحلة الثالثة: الحضور والانصراف

- تسجيل الدخول.
- تسجيل الخروج.
- منع التكرار.
- حساب مدة الدوام.
- عرض آخر عملية.

### المرحلة الرابعة: Telegram

- إعداد Bot Token وChat ID.
- إرسال رسالة نصية.
- إرسال الصورة مع الرسالة.
- إنشاء قائمة انتظار عند انقطاع الإنترنت.
- إعادة إرسال العمليات غير المرسلة.

### المرحلة الخامسة: الإدارة والتقارير

- شاشة حضور اليوم.
- البحث عن موظف.
- مراجعة الصور.
- تعديل السجلات.
- سجل التدقيق.
- تصدير Excel.
- النسخ الاحتياطي.

### المرحلة السادسة: التجهيز للنشر

- اختبار النظام.
- تحسين الأخطاء.
- دمج Java Runtime.
- إنشاء Installer.
- تشغيل تلقائي مع Windows.
- تفعيل Kiosk Mode.

---

## 17. النسخة الأولى المقترحة

يجب أن تحتوي النسخة الأولى على:

1. إدارة الموظفين.
2. إنشاء QR لكل موظف.
3. قراءة QR بالكاميرا.
4. تسجيل دخول وخروج.
5. التقاط صورة لكل عملية.
6. حفظ البيانات محليًا في SQLite.
7. إرسال الصورة والتفاصيل إلى Telegram.
8. إعادة الإرسال عند عودة الإنترنت.
9. شاشة حضور اليوم.
10. مراجعة صور العمليات.
11. تصدير Excel.
12. نسخة احتياطية تلقائية.
13. حماية لوحة الإدارة.
14. سجل للتعديلات.

---

## 18. اسم مقترح للمشروع

```text
QR Attendance Desktop
```

أو:

```text
Murakib Attendance
```

أو:

```text
Kervanji Staff Attendance
```

---

## 19. الخلاصة

النظام يعمل محليًا ولا يعتمد على وجود الإنترنت لحفظ الحضور والانصراف.

الإنترنت مطلوب فقط لإرسال إشعارات Telegram. عند انقطاعه، يتم حفظ العمليات وإرسالها لاحقًا تلقائيًا.

التكوين المقترح:

```text
JavaFX + ZXing + OpenCV + SQLite + Telegram Bot API
```

هذا التكوين مناسب لنظام Windows محلي، سريع، ومستقر، ويمكن تطويره مستقبلًا ليشمل التعرف على الوجه أو لوحة تحكم عبر الإنترنت.
